# pi-packages

This is a monorepo containing packages for the pi coding agent.

## Packages

- `packages/vscode-plugin`: VS Code extension for the pi coding

## Getting Started

To install all dependencies, run:

```bash
npm install
```
